[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "dust3r"
version = "0.1.0"
description = "DUSt3R"
dependencies = []

[tool.setuptools.packages.find]
include = ["dust3r", "dust3r.*", "datasets_preprocess", "datasets_preprocess.*"]
